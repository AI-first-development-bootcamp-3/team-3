/** Entity identifier. Opaque - treat as a string, not a number. */
export type Id = string

/** ISO 8601 date string, e.g. "2026-08-13". Convert to Date locally when needed. */
export type ISODateString = string
