# scrum-204-207-create-user-api

`POST /admin/users` — admin-only user creation with validation, password hashing, and role assignment (SCRUM-204, 205, 206, 207). Superseded by `admin-manage-users` (SCRUM-67), which absorbs this as an already-done prerequisite alongside list/edit.
