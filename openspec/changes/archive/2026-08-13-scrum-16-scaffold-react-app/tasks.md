## 1. Scaffold

- [x] 1.1 Run `npm create vite@latest frontend -- --template react-ts` at repo root
- [x] 1.2 `cd frontend && npm install`
- [x] 1.3 Remove Vite's default demo content (counter button, logos) down to a bare placeholder page

## 2. Verify

- [x] 2.1 `npm run dev` starts without errors and serves the placeholder page
- [x] 2.2 `npm run build` completes without TypeScript errors
- [x] 2.3 Commit with `frontend/.gitignore` in place (node_modules, dist excluded)
