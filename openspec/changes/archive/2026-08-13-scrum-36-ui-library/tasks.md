## 1. Install

- [x] 1.1 Install `antd`

## 2. RTL wiring

- [x] 2.1 Wrap the app in `ConfigProvider` with `direction="rtl"` and the `he_IL` locale in `main.tsx`

## 3. Prove it works

- [x] 3.1 Replace the plain `<nav>` links in `Layout.tsx` with an Ant Design component (`Menu`) to get a real rendered component, not just a placeholder
- [x] 3.2 Verify in-browser: the component renders RTL-correctly without any per-component RTL prop

## 4. Verify

- [x] 4.1 `npm run build` passes
- [x] 4.2 `npm run lint` passes
