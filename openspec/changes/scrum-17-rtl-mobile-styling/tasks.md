## 1. RTL + Hebrew

- [ ] 1.1 Set `dir="rtl"` and `lang="he"` on the `<html>` element in `index.html`

## 2. Mobile-first base styles

- [ ] 2.1 Replace the max-width breakpoint in `src/index.css` with min-width breakpoints (640px, 1024px)
- [ ] 2.2 Style the nav to wrap and have adequate touch-target sizing on narrow viewports

## 3. Verify

- [ ] 3.1 Load each page (Reports, Absences, Admin) at a mobile viewport width — nav and content usable, no horizontal scroll
- [ ] 3.2 Load each page at a desktop viewport width — layout scales up correctly
- [ ] 3.3 Confirm layout mirrors correctly for RTL (nav/text flow right-to-left)
- [ ] 3.4 `npm run build` still passes
