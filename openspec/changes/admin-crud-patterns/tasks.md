## 1. Table pattern

- [x] 1.1 Create `AdminEntityTable` wrapping antd `Table`: generic column
      defs, sortable, RTL
- [x] 1.2 Verify mobile-width rendering (responsive column handling or
      horizontal scroll contained within the component)

## 2. Form pattern

- [x] 2.1 Create `AdminEntityForm` wrapper adding an active/inactive toggle
      field to the existing schema-driven form pattern
- [x] 2.2 Add a delete-confirmation step gating deactivation submission
- [x] 2.3 Add cancel path that leaves entity state unchanged

## 3. Styling

- [ ] 3.1 Apply Figma-matched styling once the file is shared (blocked —
      see design.md)

## 4. Verification

- [x] 4.1 Confirm both patterns compile/render standalone (no consumer
      screen required yet)
- [ ] 4.2 Revisit fit once SCRUM-68 (first consuming screen) wires one of
      them in; adjust here if the pattern doesn't fit cleanly
