## 1. Install Vitest

- [ ] 1.1 Install `vitest`
- [ ] 1.2 Add `test` section to `vite.config.ts` (or a separate `vitest.config.ts`)
- [ ] 1.3 Add `test` script to `package.json`

## 2. Sample test

- [ ] 2.1 Add one trivial pure-function sample test proving the runner works

## 3. Verify

- [ ] 3.1 `npm test` runs and the sample test passes
- [ ] 3.2 `npm run build` still passes
