## 1. Scaffold

- [ ] 1.1 Run `npm create vite@latest frontend -- --template react-ts` at repo root
- [ ] 1.2 `cd frontend && npm install`
- [ ] 1.3 Remove Vite's default demo content (counter button, logos) down to a bare placeholder page

## 2. Verify

- [ ] 2.1 `npm run dev` starts without errors and serves the placeholder page
- [ ] 2.2 `npm run build` completes without TypeScript errors
- [ ] 2.3 Commit with `frontend/.gitignore` in place (node_modules, dist excluded)
