# No ^ or ~ ranges on install — every dependency is pinned to the exact
# version resolved at install time. A `npm install` never silently floats
# to a newer minor/patch; upgrades only happen when someone deliberately
# bumps a version and re-tests. package-lock.json is the second, stronger
# guarantee (see backend/README for the npm ci note for deployment).
save-exact=true
